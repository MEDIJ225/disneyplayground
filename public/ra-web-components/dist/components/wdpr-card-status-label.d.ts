import type { Components, JSX } from "../types/components";

interface WdprCardStatusLabel extends Components.WdprCardStatusLabel, HTMLElement {}
export const WdprCardStatusLabel: {
    prototype: WdprCardStatusLabel;
    new (): WdprCardStatusLabel;
};
/**
 * Used to define this component and all nested components recursively.
 */
export const defineCustomElement: () => void;
